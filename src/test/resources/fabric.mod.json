{
  "schemaVersion": 1,
  "id": "hidemodels-test",
  "version": "1.0.0",
  "name": "Hide Models client test",
  "description": "Drives a real client to check the mod works. Never shipped - this metadata lives in the test source set, so the released jar has no gametest entrypoint and no Fabric API dependency.",
  "license": "MIT",
  "environment": "client",
  "entrypoints": {
    "fabric-client-gametest": [
      "io.github.simuciokas.hidemodels.test.HideModelsClientTest"
    ]
  },
  "depends": {
    "hidemodels": "*"
  }
}
